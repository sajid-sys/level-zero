# Server configuration
PORT=8000
HOST=0.0.0.0

# LLM configuration (Optional - deterministic offline fallback runs if omitted)
GEMINI_API_KEY=
OPENAI_API_KEY=
LLM_PROVIDER=auto

# MongoDB Database Configuration
MONGODB_USERNAME=
MONGODB_PASSWORD=
MONGODB_URI=
